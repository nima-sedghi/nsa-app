# راهنمای آنلاین کردن — قدم به قدم

این یه پروژه‌ی Next.js واقعیه، با دیتابیس Postgres واقعی. رایگانه و حدود ۱۰-۱۵ دقیقه طول می‌کشه.

## قدم ۱: ساخت دیتابیس (Neon — رایگان)

1. برو به [neon.tech](https://neon.tech) و با گیت‌هاب یا گوگل ثبت‌نام کن.
2. یه پروژه‌ی جدید بساز (اسمش مهم نیست، مثلا `nazarsanji`).
3. تو داشبورد پروژه، دنبال **Connection String** بگرد. حتما نسخه‌ی **Pooled connection** رو انتخاب کن (یه چک‌باکس یا دراپ‌داون به اسم "Pooled connection" داره).
4. یه رشته شبیه این کپی کن:
   ```
   postgresql://user:password@ep-xxxx-pooler.region.aws.neon.tech/dbname?sslmode=require
   ```
   این میشه `DATABASE_URL` — جایی نگهش دار.

## قدم ۲: ساخت رمز ادمین

رو کامپیوتر خودت (نه اینجا):

```bash
cd nazarsanji-app
npm install
npm run hash-password
```

رمزی که می‌خوای رو وارد کن. دو نسخه بهت میده — یکی برای Vercel، یکی برای فایل `.env` محلی (فرقشون همینه که تو نسخه‌ی `.env` هر `$` با `\$` جایگزین شده، وگرنه Next.js موقع خوندن فایل `.env` رو `$` رو نشونه‌ی متغیر می‌گیره و هش رو خراب می‌کنه — این تو Vercel مشکلی نیست، فقط تو فایل `.env` محلی باید حواست باشه).

یه خط مثل این می‌گیری:
```
ADMIN_PASSWORD_HASH=$2a$12$....
```
این رو هم نگه دار. **خودِ رمز رو جایی ننویس، فقط این هش لازمه.**

یه رشته‌ی تصادفی هم برای امضای سشن بساز:
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```
این میشه `ADMIN_SESSION_SECRET`.

## قدم ۳: آماده‌سازی جدول‌های دیتابیس

تو همون پوشه‌ی پروژه، یه فایل `.env` بساز:
```
DATABASE_URL=همون رشته‌ای که از Neon گرفتی
ADMIN_PASSWORD_HASH=نسخه‌ی \$-دار (مخصوص .env) که تو قدم ۲ گرفتی
ADMIN_SESSION_SECRET=همون رشته‌ی تصادفی
```

بعد:
```bash
npm run db:push
```
این جدول‌ها رو تو دیتابیس Neon می‌سازه.

## قدم ۴: دیپلوی رو Vercel (رایگان)

1. کد رو تو یه ریپازیتوری GitHub بذار (اگه گیت نداری: `git init && git add . && git commit -m "init"` بعد یه ریپو خالی تو گیت‌هاب بساز و پوشش بده).
2. برو به [vercel.com](https://vercel.com)، با گیت‌هاب لاگین کن.
3. **Add New Project** → ریپازیتوری‌ت رو انتخاب کن → Import.
4. قبل از زدن Deploy، برو تو بخش **Environment Variables** و این سه‌تا رو دقیقاً با همون اسم اضافه کن (برای `ADMIN_PASSWORD_HASH` همون نسخه‌ی *بدون* `\$` رو بذار، یعنی نسخه‌ی اول که تو قدم ۲ گرفتی — اینجا مشکل escape کردن وجود نداره):
   - `DATABASE_URL`
   - `ADMIN_PASSWORD_HASH`
   - `ADMIN_SESSION_SECRET`
5. **Deploy** بزن. یکی دو دقیقه طول می‌کشه.
6. بعدش یه لینک مثل `nazarsanji-xxxx.vercel.app` بهت میده — همینه لینک نهایی که به بچه‌ها می‌دی.

## قدم ۵: تست

- برو تو لینک، `/admin` رو باز کن، با همون رمزی که تو قدم ۲ ساختی وارد شو.
- یه درس تستی بساز، یا فایل‌های xlsx آموزشیار رو آپلود کن.
- از یه مرورگر یا گوشی دیگه (یا حالت ناشناس) برو تو صفحه اصلی و رای بده، ببین نتیجه درست ثبت میشه.

## نکات مهم

- **دامنه‌ی دلخواه:** تو تنظیمات Vercel، بخش Domains، می‌تونی یه دامنه‌ی رایگان‌تر یا حتی دامنه‌ی شخصی وصل کنی.
- **رمز ادمین رو عوض کردن:** دوباره `npm run hash-password` بزن، هش جدید رو تو Environment Variables ورسل جایگزین کن، Redeploy بزن.
- **بک‌آپ گرفتن از داده‌ها:** از داشبورد Neon می‌تونی هر وقت خواستی از کل دیتابیس یه Snapshot/Export بگیری.
- **اگه یه روز بخوای مقیاسش رو ببری بالا:** تا سقف رایگان Neon (چند گیگ) و Vercel (Hobby plan) این پروژه بدون هیچ تغییری جواب میده. برای دانشکده/دانشگاه معمولاً کافیه.
